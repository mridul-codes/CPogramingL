// write a program repeating a sentence using do while loop:
#include <stdio.h>

    int
    main()
{
    int i = 0;
    do
    {
        printf("Here we go again\t\n");
        i++;
    }
    while (i<15);

    return 0;
}