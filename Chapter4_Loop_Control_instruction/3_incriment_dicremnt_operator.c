#include<stdio.h>

int main()
{
    int a=5;
    int i=3;
    printf("The value of a++ is %d\n", a++);
    //  a++ means here it the value of a then increment
    printf("The value of a is %d\n", a); // new value of a is assigend from the line 7
    printf("The value of ++i is %d\n", ++i);
    // if ++i then first increment than printing out the value
    //similarly togle the negative sign
    return 0;
}
// as like that += means like a += b is short from off a = a +b