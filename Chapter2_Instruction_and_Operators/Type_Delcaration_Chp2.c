#include<stdio.h>

int main()
{
    
    float a = 3.1;
    float b = a + 6.9;
    int k = 3.0 / 9 ;
    // note that if we write the b at first and then a at second it will throw us an error
    // the reason for it because we have ask to pogram about the thing before the declaration 
    // note that here a = 3.1 is a type delaration
    printf("the value of b is %f\n", b );
    return 0;
}